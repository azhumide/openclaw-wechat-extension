"use strict";Object.defineProperty(exports, "__esModule", { value: true });exports.t = checkMinHostVersion;var _stringCoerceCjxCKZ6B = require("./string-coerce-CjxCKZ6B.js");
var _runtimeGuardDYXyo2C = require("./runtime-guard-DYXyo2C1.js");
//#region src/plugins/min-host-version.ts
const MIN_HOST_VERSION_FORMAT = "openclaw.install.minHostVersion must use a semver floor in the form \">=x.y.z\"";
const MIN_HOST_VERSION_RE = /^>=(\d+)\.(\d+)\.(\d+)$/;
function parseMinHostVersionRequirement(raw) {
  if (typeof raw !== "string") return null;
  const trimmed = raw.trim();
  if (!trimmed) return null;
  const match = trimmed.match(MIN_HOST_VERSION_RE);
  if (!match) return null;
  const minimumLabel = `${match[1]}.${match[2]}.${match[3]}`;
  if (!(0, _runtimeGuardDYXyo2C.a)(minimumLabel)) return null;
  return {
    raw: trimmed,
    minimumLabel
  };
}
function checkMinHostVersion(params) {
  if (params.minHostVersion === void 0) return {
    ok: true,
    requirement: null
  };
  const requirement = parseMinHostVersionRequirement(params.minHostVersion);
  if (!requirement) return {
    ok: false,
    kind: "invalid",
    error: MIN_HOST_VERSION_FORMAT
  };
  const currentVersion = (0, _stringCoerceCjxCKZ6B.c)(params.currentVersion) || "unknown";
  const currentSemver = (0, _runtimeGuardDYXyo2C.a)(currentVersion);
  if (!currentSemver) return {
    ok: false,
    kind: "unknown_host_version",
    requirement
  };
  if (!(0, _runtimeGuardDYXyo2C.n)(currentSemver, (0, _runtimeGuardDYXyo2C.a)(requirement.minimumLabel))) return {
    ok: false,
    kind: "incompatible",
    requirement,
    currentVersion
  };
  return {
    ok: true,
    requirement
  };
}
//#endregion /* v9-a2f79c3bd99ed9e9 */
