"use strict";Object.defineProperty(exports, "__esModule", { value: true });exports.a = normalizeAnyChannelId;exports.c = listChatChannels;exports.i = listRegisteredChannelPluginIds;exports.l = buildChatChannelMetaById;exports.n = formatChannelSelectionLine;exports.o = normalizeChannelId;exports.r = getRegisteredChannelPluginMeta;exports.s = getChatChannelMeta;exports.t = formatChannelPrimerLine;var _stringCoerceC1IzJjqi = require("./string-coerce-C1IzJjqi.js");
var _ids2bZIXJNM = require("./ids-2bZIXJNM.js");
var _runtimeChannelStateD5dsafJk = require("./runtime-channel-state-D5dsafJk.js");
var _channelMetaBEaGWV3f = require("./channel-meta-BEaGWV3f.js");
//#region src/channels/chat-meta-shared.ts
const CHAT_CHANNEL_ID_SET = new Set(_ids2bZIXJNM.n);
function toChatChannelMeta(params) {
  const label = (0, _stringCoerceC1IzJjqi.c)(params.channel.label);
  if (!label) throw new Error(`Missing label for bundled chat channel "${params.id}"`);
  return (0, _channelMetaBEaGWV3f.t)({
    id: params.id,
    channel: params.channel,
    label,
    selectionLabel: (0, _stringCoerceC1IzJjqi.c)(params.channel.selectionLabel) || label,
    docsPath: (0, _stringCoerceC1IzJjqi.c)(params.channel.docsPath) || `/channels/${params.id}`,
    docsLabel: (0, _stringCoerceC1IzJjqi.c)(params.channel.docsLabel),
    blurb: (0, _stringCoerceC1IzJjqi.c)(params.channel.blurb) || "",
    detailLabel: (0, _stringCoerceC1IzJjqi.c)(params.channel.detailLabel),
    systemImage: (0, _stringCoerceC1IzJjqi.c)(params.channel.systemImage),
    arrayFieldMode: "non-empty",
    selectionDocsPrefixMode: "defined"
  });
}
function buildChatChannelMetaById() {
  const entries = /* @__PURE__ */new Map();
  for (const entry of (0, _ids2bZIXJNM.i)()) {
    const rawId = (0, _stringCoerceC1IzJjqi.c)(entry.id);
    if (!rawId || !CHAT_CHANNEL_ID_SET.has(rawId)) continue;
    const id = rawId;
    entries.set(id, toChatChannelMeta({
      id,
      channel: entry.channel
    }));
  }
  return Object.freeze(Object.fromEntries(entries));
}
//#endregion
//#region src/channels/chat-meta.ts
const CHAT_CHANNEL_META = buildChatChannelMetaById();
function listChatChannels() {
  return _ids2bZIXJNM.n.map((id) => CHAT_CHANNEL_META[id]);
}
function getChatChannelMeta(id) {
  return CHAT_CHANNEL_META[id];
}
//#endregion
//#region src/channels/registry.ts
function listRegisteredChannelPluginEntries() {
  const channelRegistry = (0, _runtimeChannelStateD5dsafJk.t)();
  if (channelRegistry && channelRegistry.channels && channelRegistry.channels.length > 0) return channelRegistry.channels;
  return [];
}
function findRegisteredChannelPluginEntry(normalizedKey) {
  return listRegisteredChannelPluginEntries().find((entry) => {
    const id = (0, _stringCoerceC1IzJjqi.s)(entry.plugin.id ?? "") ?? "";
    if (id && id === normalizedKey) return true;
    return (entry.plugin.meta?.aliases ?? []).some((alias) => (0, _stringCoerceC1IzJjqi.s)(alias) === normalizedKey);
  });
}
function findRegisteredChannelPluginEntryById(id) {
  const normalizedId = (0, _stringCoerceC1IzJjqi.s)(id);
  if (!normalizedId) return;
  return listRegisteredChannelPluginEntries().find((entry) => (0, _stringCoerceC1IzJjqi.s)(entry.plugin.id) === normalizedId);
}
function normalizeChannelId(raw) {
  return (0, _ids2bZIXJNM.r)(raw);
}
function normalizeAnyChannelId(raw) {
  const key = (0, _stringCoerceC1IzJjqi.s)(raw);
  if (!key) return null;
  return findRegisteredChannelPluginEntry(key)?.plugin.id ?? null;
}
function listRegisteredChannelPluginIds() {
  return listRegisteredChannelPluginEntries().flatMap((entry) => {
    const id = (0, _stringCoerceC1IzJjqi.c)(entry.plugin.id);
    return id ? [id] : [];
  });
}
function getRegisteredChannelPluginMeta(id) {
  return findRegisteredChannelPluginEntryById(id)?.plugin.meta ?? null;
}
function formatChannelPrimerLine(meta) {
  return `${meta.label}: ${meta.blurb}`;
}
function formatChannelSelectionLine(meta, docsLink) {
  const docsPrefix = meta.selectionDocsPrefix ?? "Docs:";
  const docsLabel = meta.docsLabel ?? meta.id;
  const docs = meta.selectionDocsOmitLabel ? docsLink(meta.docsPath) : docsLink(meta.docsPath, docsLabel);
  const extras = (meta.selectionExtras ?? []).filter(Boolean).join(" ");
  return `${meta.label} — ${meta.blurb} ${docsPrefix ? `${docsPrefix} ` : ""}${docs}${extras ? ` ${extras}` : ""}`;
}
//#endregion /* v9-7ff157cd8827e2d4 */
