"use strict";Object.defineProperty(exports, "__esModule", { value: true });exports.C = resolveCanonicalRootMemoryFile;exports.D = shouldSkipRootMemoryAuxiliaryPath;exports.E = resolveRootMemoryRepairDir;exports.S = void 0;exports.T = resolveLegacyRootMemoryPath;exports._ = reconcileWorkspaceBootstrapCompletion;exports.a = void 0;exports.b = resolveWorkspaceTemplateDir;exports.c = void 0;exports.d = filterBootstrapFilesForSession;exports.f = isWorkspaceBootstrapPending;exports.g = loadWorkspaceBootstrapFiles;exports.h = loadExtraBootstrapFilesWithDiagnostics;exports.l = exports.i = void 0;exports.m = loadExtraBootstrapFiles;exports.o = exports.n = void 0;exports.p = isWorkspaceSetupCompleted;exports.t = exports.s = exports.r = void 0;exports.u = ensureAgentWorkspace;exports.v = resolveDefaultAgentWorkspaceDir;exports.w = resolveCanonicalRootMemoryPath;exports.x = void 0;exports.y = resolveWorkspaceBootstrapStatus;var _stringCoerceC1IzJjqi = require("./string-coerce-C1IzJjqi.js");
var _homeDirCfZ08GnU = require("./home-dir-CfZ08GnU.js");
var _openclawRootBNWw3cXT = require("./openclaw-root-BNWw3cXT.js");
var _utilsBMRcljdi = require("./utils-BMRcljdi.js");
var _execBaXt0vsL = require("./exec-BaXt0vsL.js");
var _boundaryFileReadBDEvfiRb = require("./boundary-file-read-BDEvfiRb.js");
var _sessionKeyUtilsBT0y7mVK = require("./session-key-utils-BT0y7mVK.js");
var _nodeUrl = require("node:url");
var _nodeFs = _interopRequireDefault(require("node:fs"));
var _nodePath = _interopRequireDefault(require("node:path"));
var _nodeOs = _interopRequireDefault(require("node:os"));
var _promises = _interopRequireDefault(require("node:fs/promises"));function _interopRequireDefault(e) {return e && e.__esModule ? e : { default: e };}
//#region src/memory/root-memory-files.ts
const CANONICAL_ROOT_MEMORY_FILENAME = exports.x = "MEMORY.md";
const LEGACY_ROOT_MEMORY_FILENAME = exports.S = "memory.md";
function resolveCanonicalRootMemoryPath(workspaceDir) {
  return _nodePath.default.join(workspaceDir, CANONICAL_ROOT_MEMORY_FILENAME);
}
function resolveLegacyRootMemoryPath(workspaceDir) {
  return _nodePath.default.join(workspaceDir, LEGACY_ROOT_MEMORY_FILENAME);
}
function resolveRootMemoryRepairDir(workspaceDir) {
  return _nodePath.default.join(workspaceDir, ".openclaw-repair", "root-memory");
}
function normalizeWorkspaceRelativePath(value) {
  return value.trim().replace(/\\/g, "/").replace(/^\.\//, "");
}
async function exactWorkspaceEntryExists(dir, name) {
  try {
    return (await _promises.default.readdir(dir)).includes(name);
  } catch {
    return false;
  }
}
async function resolveCanonicalRootMemoryFile(workspaceDir) {
  try {
    const entries = await _promises.default.readdir(workspaceDir, { withFileTypes: true });
    for (const entry of entries) if (entry.name === "MEMORY.md" && entry.isFile() && !entry.isSymbolicLink()) return _nodePath.default.join(workspaceDir, entry.name);
  } catch {}
  return null;
}
function shouldSkipRootMemoryAuxiliaryPath(params) {
  const relative = _nodePath.default.relative(params.workspaceDir, params.absPath);
  if (relative.startsWith("..") || _nodePath.default.isAbsolute(relative)) return false;
  const normalized = normalizeWorkspaceRelativePath(relative);
  return normalized === "memory.md" || normalized === ".openclaw-repair/root-memory" || normalized.startsWith(`.openclaw-repair/root-memory/`);
}
//#endregion
//#region src/agents/workspace-templates.ts
const FALLBACK_TEMPLATE_DIR = _nodePath.default.resolve(_nodePath.default.dirname((0, _nodeUrl.fileURLToPath)("file:///home/rs/openclaw/dist/workspace-DiB6mKpP.js")), "../../docs/reference/templates");
let cachedTemplateDir;
let resolvingTemplateDir;
async function resolveWorkspaceTemplateDir(opts) {
  if (cachedTemplateDir) return cachedTemplateDir;
  if (resolvingTemplateDir) return resolvingTemplateDir;
  resolvingTemplateDir = (async () => {
    const moduleUrl = opts?.moduleUrl ?? "file:///home/rs/openclaw/dist/workspace-DiB6mKpP.js";
    const argv1 = opts?.argv1 ?? process.argv[1];
    const cwd = opts?.cwd ?? process.cwd();
    const packageRoot = await (0, _openclawRootBNWw3cXT.t)({
      moduleUrl,
      argv1,
      cwd
    });
    const candidates = [
    packageRoot ? _nodePath.default.join(packageRoot, "docs", "reference", "templates") : null,
    cwd ? _nodePath.default.resolve(cwd, "docs", "reference", "templates") : null,
    FALLBACK_TEMPLATE_DIR].
    filter(Boolean);
    for (const candidate of candidates) if (await (0, _utilsBMRcljdi.d)(candidate)) {
      cachedTemplateDir = candidate;
      return candidate;
    }
    cachedTemplateDir = candidates[0] ?? FALLBACK_TEMPLATE_DIR;
    return cachedTemplateDir;
  })();
  try {
    return await resolvingTemplateDir;
  } finally {
    resolvingTemplateDir = void 0;
  }
}
//#endregion
//#region src/agents/workspace.ts
function resolveDefaultAgentWorkspaceDir(env = process.env, homedir = _nodeOs.default.homedir) {
  const home = (0, _homeDirCfZ08GnU.o)(env, homedir);
  const profile = env.OPENCLAW_PROFILE?.trim();
  if (profile && (0, _stringCoerceC1IzJjqi.s)(profile) !== "default") return _nodePath.default.join(home, ".openclaw", `workspace-${profile}`);
  return _nodePath.default.join(home, ".openclaw", "workspace");
}
const DEFAULT_AGENT_WORKSPACE_DIR = exports.n = resolveDefaultAgentWorkspaceDir();
const DEFAULT_AGENTS_FILENAME = exports.t = "AGENTS.md";
const DEFAULT_SOUL_FILENAME = exports.s = "SOUL.md";
const DEFAULT_TOOLS_FILENAME = exports.c = "TOOLS.md";
const DEFAULT_IDENTITY_FILENAME = exports.a = "IDENTITY.md";
const DEFAULT_USER_FILENAME = exports.l = "USER.md";
const DEFAULT_HEARTBEAT_FILENAME = exports.i = "HEARTBEAT.md";
const DEFAULT_BOOTSTRAP_FILENAME = exports.r = "BOOTSTRAP.md";
const DEFAULT_MEMORY_FILENAME = exports.o = CANONICAL_ROOT_MEMORY_FILENAME;
const WORKSPACE_STATE_DIRNAME = ".openclaw";
const WORKSPACE_STATE_FILENAME = "workspace-state.json";
const WORKSPACE_STATE_VERSION = 1;
const WORKSPACE_ONBOARDING_PROFILE_FILENAMES = [
DEFAULT_SOUL_FILENAME,
DEFAULT_IDENTITY_FILENAME,
DEFAULT_USER_FILENAME];

const workspaceTemplateCache = /* @__PURE__ */new Map();
let gitAvailabilityPromise = null;
const MAX_WORKSPACE_BOOTSTRAP_FILE_BYTES = 2 * 1024 * 1024;
const workspaceFileCache = /* @__PURE__ */new Map();
function workspaceFileIdentity(stat, canonicalPath) {
  return `${canonicalPath}|${stat.dev}:${stat.ino}:${stat.size}:${stat.mtimeMs}`;
}
async function readWorkspaceFileWithGuards(params) {
  const opened = await (0, _boundaryFileReadBDEvfiRb.r)({
    absolutePath: params.filePath,
    rootPath: params.workspaceDir,
    boundaryLabel: "workspace root",
    maxBytes: MAX_WORKSPACE_BOOTSTRAP_FILE_BYTES
  });
  if (!opened.ok) {
    workspaceFileCache.delete(params.filePath);
    return opened;
  }
  const identity = workspaceFileIdentity(opened.stat, opened.path);
  const cached = workspaceFileCache.get(params.filePath);
  if (cached && cached.identity === identity) {
    _nodeFs.default.closeSync(opened.fd);
    return {
      ok: true,
      content: cached.content
    };
  }
  try {
    const content = _nodeFs.default.readFileSync(opened.fd, "utf-8");
    workspaceFileCache.set(params.filePath, {
      content,
      identity
    });
    return {
      ok: true,
      content
    };
  } catch (error) {
    workspaceFileCache.delete(params.filePath);
    return {
      ok: false,
      reason: "io",
      error
    };
  } finally {
    _nodeFs.default.closeSync(opened.fd);
  }
}
function stripFrontMatter(content) {
  if (!content.startsWith("---")) return content;
  const endIndex = content.indexOf("\n---", 3);
  if (endIndex === -1) return content;
  const start = endIndex + 4;
  let trimmed = content.slice(start);
  trimmed = trimmed.replace(/^\s+/, "");
  return trimmed;
}
async function loadTemplate(name) {
  const cached = workspaceTemplateCache.get(name);
  if (cached) return cached;
  const pending = (async () => {
    const templateDir = await resolveWorkspaceTemplateDir();
    const templatePath = _nodePath.default.join(templateDir, name);
    try {
      return stripFrontMatter(await _promises.default.readFile(templatePath, "utf-8"));
    } catch {
      throw new Error(`Missing workspace template: ${name} (${templatePath}). Ensure docs/reference/templates are packaged.`);
    }
  })();
  workspaceTemplateCache.set(name, pending);
  try {
    return await pending;
  } catch (error) {
    workspaceTemplateCache.delete(name);
    throw error;
  }
}
/** Set of recognized bootstrap filenames for runtime validation */
const VALID_BOOTSTRAP_NAMES = new Set([
DEFAULT_AGENTS_FILENAME,
DEFAULT_SOUL_FILENAME,
DEFAULT_TOOLS_FILENAME,
DEFAULT_IDENTITY_FILENAME,
DEFAULT_USER_FILENAME,
DEFAULT_HEARTBEAT_FILENAME,
DEFAULT_BOOTSTRAP_FILENAME,
DEFAULT_MEMORY_FILENAME]
);
async function writeFileIfMissing(filePath, content) {
  try {
    await _promises.default.writeFile(filePath, content, {
      encoding: "utf-8",
      flag: "wx"
    });
    return true;
  } catch (err) {
    if (err.code !== "EEXIST") throw err;
    return false;
  }
}
async function fileExists(filePath) {
  try {
    await _promises.default.access(filePath);
    return true;
  } catch {
    return false;
  }
}
async function fileContentDiffersFromTemplate(filePath, template) {
  try {
    return (await _promises.default.readFile(filePath, "utf-8")) !== template;
  } catch (err) {
    if (err.code !== "ENOENT") throw err;
    return false;
  }
}
async function hasWorkspaceUserContentEvidence(dir, opts) {
  const indicators = [_nodePath.default.join(dir, "memory")];
  if (opts?.includeGit) indicators.push(_nodePath.default.join(dir, ".git"));
  for (const indicator of indicators) try {
    await _promises.default.access(indicator);
    return true;
  } catch {}
  return await exactWorkspaceEntryExists(dir, DEFAULT_MEMORY_FILENAME);
}
async function workspaceProfileLooksConfigured(params) {
  return (await Promise.all(WORKSPACE_ONBOARDING_PROFILE_FILENAMES.map(async (fileName) => fileContentDiffersFromTemplate(_nodePath.default.join(params.dir, fileName), await loadTemplate(fileName))))).some(Boolean) || (await hasWorkspaceUserContentEvidence(params.dir, { includeGit: params.includeGitEvidence }));
}
async function workspaceHasBootstrapCompletionEvidence(params) {
  return await workspaceProfileLooksConfigured(params);
}
async function reconcileWorkspaceBootstrapCompletionState(params) {
  const bootstrapExists = params.bootstrapExists ?? (await fileExists(params.bootstrapPath));
  if (typeof params.state.setupCompletedAt === "string" && params.state.setupCompletedAt.trim().length > 0) return {
    repaired: false,
    bootstrapExists,
    state: params.state
  };
  if (params.state.bootstrapSeededAt && !bootstrapExists) {
    const completedState = {
      ...params.state,
      setupCompletedAt: (/* @__PURE__ */new Date()).toISOString()
    };
    await writeWorkspaceSetupState(params.statePath, completedState);
    return {
      repaired: true,
      bootstrapExists: false,
      state: completedState
    };
  }
  if (!bootstrapExists || !(await workspaceHasBootstrapCompletionEvidence({ dir: params.dir }))) return {
    repaired: false,
    bootstrapExists,
    state: params.state
  };
  const now = (/* @__PURE__ */new Date()).toISOString();
  const repairedState = {
    ...params.state,
    bootstrapSeededAt: params.state.bootstrapSeededAt ?? now,
    setupCompletedAt: now
  };
  await _promises.default.rm(params.bootstrapPath, { force: true });
  await writeWorkspaceSetupState(params.statePath, repairedState);
  return {
    repaired: true,
    bootstrapExists: false,
    state: repairedState
  };
}
function resolveWorkspaceStatePath(dir) {
  return _nodePath.default.join(dir, WORKSPACE_STATE_DIRNAME, WORKSPACE_STATE_FILENAME);
}
function parseWorkspaceSetupState(raw) {
  try {
    const parsed = JSON.parse(raw);
    if (!parsed || typeof parsed !== "object") return null;
    const legacyCompletedAt = (0, _stringCoerceC1IzJjqi.f)(parsed.onboardingCompletedAt);
    return {
      version: WORKSPACE_STATE_VERSION,
      bootstrapSeededAt: (0, _stringCoerceC1IzJjqi.f)(parsed.bootstrapSeededAt),
      setupCompletedAt: (0, _stringCoerceC1IzJjqi.f)(parsed.setupCompletedAt) ?? legacyCompletedAt
    };
  } catch {
    return null;
  }
}
async function readWorkspaceSetupState(statePath, opts) {
  try {
    const raw = await _promises.default.readFile(statePath, "utf-8");
    const parsed = parseWorkspaceSetupState(raw);
    if (opts?.persistLegacyMigration && parsed && raw.includes("\"onboardingCompletedAt\"") && !raw.includes("\"setupCompletedAt\"") && parsed.setupCompletedAt) await writeWorkspaceSetupState(statePath, parsed);
    return parsed ?? { version: WORKSPACE_STATE_VERSION };
  } catch (err) {
    if (err.code !== "ENOENT") throw err;
    return { version: WORKSPACE_STATE_VERSION };
  }
}
async function readWorkspaceSetupStateForDir(dir) {
  return await readWorkspaceSetupState(resolveWorkspaceStatePath((0, _utilsBMRcljdi.m)(dir)));
}
async function isWorkspaceSetupCompleted(dir) {
  const state = await readWorkspaceSetupStateForDir(dir);
  return typeof state.setupCompletedAt === "string" && state.setupCompletedAt.trim().length > 0;
}
async function resolveWorkspaceBootstrapStatus(dir) {
  const resolvedDir = (0, _utilsBMRcljdi.m)(dir);
  const state = await readWorkspaceSetupState(resolveWorkspaceStatePath(resolvedDir));
  if (typeof state.setupCompletedAt === "string" && state.setupCompletedAt.trim().length > 0) return "complete";
  if (!(await fileExists(_nodePath.default.join(resolvedDir, "BOOTSTRAP.md")))) return "complete";
  return "pending";
}
async function isWorkspaceBootstrapPending(dir) {
  return (await resolveWorkspaceBootstrapStatus(dir)) === "pending";
}
async function reconcileWorkspaceBootstrapCompletion(dir) {
  const resolvedDir = (0, _utilsBMRcljdi.m)(dir);
  const statePath = resolveWorkspaceStatePath(resolvedDir);
  return await reconcileWorkspaceBootstrapCompletionState({
    dir: resolvedDir,
    bootstrapPath: _nodePath.default.join(resolvedDir, DEFAULT_BOOTSTRAP_FILENAME),
    statePath,
    state: await readWorkspaceSetupState(statePath, { persistLegacyMigration: true })
  });
}
async function writeWorkspaceSetupState(statePath, state) {
  await _promises.default.mkdir(_nodePath.default.dirname(statePath), { recursive: true });
  const payload = `${JSON.stringify(state, null, 2)}\n`;
  const tmpPath = `${statePath}.tmp-${process.pid}-${Date.now().toString(36)}`;
  try {
    await _promises.default.writeFile(tmpPath, payload, { encoding: "utf-8" });
    await _promises.default.rename(tmpPath, statePath);
  } catch (err) {
    await _promises.default.unlink(tmpPath).catch(() => {});
    throw err;
  }
}
async function hasGitRepo(dir) {
  try {
    await _promises.default.stat(_nodePath.default.join(dir, ".git"));
    return true;
  } catch {
    return false;
  }
}
async function isGitAvailable() {
  if (gitAvailabilityPromise) return gitAvailabilityPromise;
  gitAvailabilityPromise = (async () => {
    try {
      return (await (0, _execBaXt0vsL.r)(["git", "--version"], { timeoutMs: 2e3 })).code === 0;
    } catch {
      return false;
    }
  })();
  return gitAvailabilityPromise;
}
async function ensureGitRepo(dir, isBrandNewWorkspace) {
  if (!isBrandNewWorkspace) return;
  if (await hasGitRepo(dir)) return;
  if (!(await isGitAvailable())) return;
  try {
    await (0, _execBaXt0vsL.r)(["git", "init"], {
      cwd: dir,
      timeoutMs: 1e4
    });
  } catch {}
}
async function ensureAgentWorkspace(params) {
  const dir = (0, _utilsBMRcljdi.m)(params?.dir?.trim() ? params.dir.trim() : DEFAULT_AGENT_WORKSPACE_DIR);
  await _promises.default.mkdir(dir, { recursive: true });
  if (!params?.ensureBootstrapFiles) return { dir };
  const agentsPath = _nodePath.default.join(dir, DEFAULT_AGENTS_FILENAME);
  const soulPath = _nodePath.default.join(dir, DEFAULT_SOUL_FILENAME);
  const toolsPath = _nodePath.default.join(dir, DEFAULT_TOOLS_FILENAME);
  const identityPath = _nodePath.default.join(dir, DEFAULT_IDENTITY_FILENAME);
  const userPath = _nodePath.default.join(dir, DEFAULT_USER_FILENAME);
  const heartbeatPath = _nodePath.default.join(dir, DEFAULT_HEARTBEAT_FILENAME);
  const bootstrapPath = _nodePath.default.join(dir, DEFAULT_BOOTSTRAP_FILENAME);
  const statePath = resolveWorkspaceStatePath(dir);
  const isBrandNewWorkspace = await (async () => {
    const templatePaths = [
    agentsPath,
    soulPath,
    toolsPath,
    identityPath,
    userPath,
    heartbeatPath];

    const userContentPaths = [_nodePath.default.join(dir, "memory"), _nodePath.default.join(dir, ".git")];
    const paths = [...templatePaths, ...userContentPaths];
    const existing = await Promise.all(paths.map(async (p) => {
      try {
        await _promises.default.access(p);
        return true;
      } catch {
        return false;
      }
    }));
    const hasCanonicalRootMemory = await exactWorkspaceEntryExists(dir, DEFAULT_MEMORY_FILENAME);
    return existing.every((v) => !v) && !hasCanonicalRootMemory;
  })();
  const agentsTemplate = await loadTemplate(DEFAULT_AGENTS_FILENAME);
  const soulTemplate = await loadTemplate(DEFAULT_SOUL_FILENAME);
  const toolsTemplate = await loadTemplate(DEFAULT_TOOLS_FILENAME);
  const identityTemplate = await loadTemplate(DEFAULT_IDENTITY_FILENAME);
  const userTemplate = await loadTemplate(DEFAULT_USER_FILENAME);
  const heartbeatTemplate = await loadTemplate(DEFAULT_HEARTBEAT_FILENAME);
  await writeFileIfMissing(agentsPath, agentsTemplate);
  await writeFileIfMissing(soulPath, soulTemplate);
  await writeFileIfMissing(toolsPath, toolsTemplate);
  const identityPathCreated = await writeFileIfMissing(identityPath, identityTemplate);
  await writeFileIfMissing(userPath, userTemplate);
  await writeFileIfMissing(heartbeatPath, heartbeatTemplate);
  let state = await readWorkspaceSetupState(statePath, { persistLegacyMigration: true });
  let stateDirty = false;
  const markState = (next) => {
    state = {
      ...state,
      ...next
    };
    stateDirty = true;
  };
  const nowIso = () => (/* @__PURE__ */new Date()).toISOString();
  let bootstrapExists = await fileExists(bootstrapPath);
  if (!state.bootstrapSeededAt && bootstrapExists) markState({ bootstrapSeededAt: nowIso() });
  if (!state.setupCompletedAt) {
    const repair = await reconcileWorkspaceBootstrapCompletionState({
      dir,
      bootstrapPath,
      statePath,
      state,
      bootstrapExists
    });
    if (repair.repaired) {
      state = repair.state;
      stateDirty = false;
      bootstrapExists = repair.bootstrapExists;
    }
  }
  if (!state.bootstrapSeededAt && !state.setupCompletedAt && !bootstrapExists) if (await workspaceProfileLooksConfigured({
    dir,
    includeGitEvidence: true
  })) markState({ setupCompletedAt: nowIso() });else
  {
    if (!(await writeFileIfMissing(bootstrapPath, await loadTemplate("BOOTSTRAP.md")))) bootstrapExists = await fileExists(bootstrapPath);else
    bootstrapExists = true;
    if (bootstrapExists && !state.bootstrapSeededAt) markState({ bootstrapSeededAt: nowIso() });
  }
  if (stateDirty) await writeWorkspaceSetupState(statePath, state);
  await ensureGitRepo(dir, isBrandNewWorkspace);
  return {
    dir,
    agentsPath,
    soulPath,
    toolsPath,
    identityPath,
    userPath,
    heartbeatPath,
    bootstrapPath,
    identityPathCreated
  };
}
async function loadWorkspaceBootstrapFiles(dir) {
  const resolvedDir = (0, _utilsBMRcljdi.m)(dir);
  const entries = [
  {
    name: DEFAULT_AGENTS_FILENAME,
    filePath: _nodePath.default.join(resolvedDir, DEFAULT_AGENTS_FILENAME)
  },
  {
    name: DEFAULT_SOUL_FILENAME,
    filePath: _nodePath.default.join(resolvedDir, DEFAULT_SOUL_FILENAME)
  },
  {
    name: DEFAULT_TOOLS_FILENAME,
    filePath: _nodePath.default.join(resolvedDir, DEFAULT_TOOLS_FILENAME)
  },
  {
    name: DEFAULT_IDENTITY_FILENAME,
    filePath: _nodePath.default.join(resolvedDir, DEFAULT_IDENTITY_FILENAME)
  },
  {
    name: DEFAULT_USER_FILENAME,
    filePath: _nodePath.default.join(resolvedDir, DEFAULT_USER_FILENAME)
  },
  {
    name: DEFAULT_HEARTBEAT_FILENAME,
    filePath: _nodePath.default.join(resolvedDir, DEFAULT_HEARTBEAT_FILENAME)
  },
  {
    name: DEFAULT_BOOTSTRAP_FILENAME,
    filePath: _nodePath.default.join(resolvedDir, DEFAULT_BOOTSTRAP_FILENAME)
  },
  {
    name: DEFAULT_MEMORY_FILENAME,
    filePath: _nodePath.default.join(resolvedDir, DEFAULT_MEMORY_FILENAME)
  }];

  const result = [];
  for (const entry of entries) {
    if (entry.name === DEFAULT_MEMORY_FILENAME && !(await exactWorkspaceEntryExists(resolvedDir, DEFAULT_MEMORY_FILENAME))) continue;
    const loaded = await readWorkspaceFileWithGuards({
      filePath: entry.filePath,
      workspaceDir: resolvedDir
    });
    if (loaded.ok) result.push({
      name: entry.name,
      path: entry.filePath,
      content: loaded.content,
      missing: false
    });else
    result.push({
      name: entry.name,
      path: entry.filePath,
      missing: true
    });
  }
  return result;
}
const MINIMAL_BOOTSTRAP_ALLOWLIST = new Set([
DEFAULT_AGENTS_FILENAME,
DEFAULT_TOOLS_FILENAME,
DEFAULT_SOUL_FILENAME,
DEFAULT_IDENTITY_FILENAME,
DEFAULT_USER_FILENAME]
);
function filterBootstrapFilesForSession(files, sessionKey) {
  if (!sessionKey || !(0, _sessionKeyUtilsBT0y7mVK.a)(sessionKey) && !(0, _sessionKeyUtilsBT0y7mVK.i)(sessionKey)) return files;
  return files.filter((file) => MINIMAL_BOOTSTRAP_ALLOWLIST.has(file.name));
}
async function loadExtraBootstrapFiles(dir, extraPatterns) {
  return (await loadExtraBootstrapFilesWithDiagnostics(dir, extraPatterns)).files;
}
async function loadExtraBootstrapFilesWithDiagnostics(dir, extraPatterns) {
  if (!extraPatterns.length) return {
    files: [],
    diagnostics: []
  };
  const resolvedDir = (0, _utilsBMRcljdi.m)(dir);
  const resolvedPaths = /* @__PURE__ */new Set();
  for (const pattern of extraPatterns) if (pattern.includes("*") || pattern.includes("?") || pattern.includes("{")) try {
    const matches = _promises.default.glob(pattern, { cwd: resolvedDir });
    for await (const m of matches) resolvedPaths.add(m);
  } catch {
    resolvedPaths.add(pattern);
  } else
  resolvedPaths.add(pattern);
  const files = [];
  const diagnostics = [];
  for (const relPath of resolvedPaths) {
    const filePath = _nodePath.default.resolve(resolvedDir, relPath);
    const baseName = _nodePath.default.basename(relPath);
    if (!VALID_BOOTSTRAP_NAMES.has(baseName)) {
      diagnostics.push({
        path: filePath,
        reason: "invalid-bootstrap-filename",
        detail: `unsupported bootstrap basename: ${baseName}`
      });
      continue;
    }
    const loaded = await readWorkspaceFileWithGuards({
      filePath,
      workspaceDir: resolvedDir
    });
    if (loaded.ok) {
      files.push({
        name: baseName,
        path: filePath,
        content: loaded.content,
        missing: false
      });
      continue;
    }
    const reason = loaded.reason === "path" ? "missing" : loaded.reason === "validation" ? "security" : "io";
    diagnostics.push({
      path: filePath,
      reason,
      detail: loaded.error instanceof Error ? loaded.error.message : typeof loaded.error === "string" ? loaded.error : reason
    });
  }
  return {
    files,
    diagnostics
  };
}
//#endregion /* v9-fd2e4cde8f9351ae */
